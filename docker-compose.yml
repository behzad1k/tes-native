services:
  tes-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tes-native:latest
    container_name: tes-native
    ports:
      - "3003:3003"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Removed env_file and volume since .env is baked into build
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3003"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - tes-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  tes-network:
    driver: bridge
